# Build-time only. The shipped app has no Python runtime at all — these packages exist
# to export the classifier to ONNX, quantise it and measure accuracy.
torch==2.5.1
torchvision==0.20.1
transformers==4.47.1
onnx==1.17.0
onnxruntime==1.20.1
pillow==11.0.0
numpy==1.26.4
requests==2.32.3
